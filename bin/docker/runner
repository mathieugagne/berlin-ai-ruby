#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../../lib/docker_compose"

command_line = "rdbg --nonstop --open --host 0.0.0.0 --port 4567 -c --" if ARGV.delete("--debug")
DockerCompose.exec(service: "runner", command_line: command_line)
